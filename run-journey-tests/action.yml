name: Run Journey Tests on GitHub

inputs:
  epr-register-enrol-be:
    description: epr-register-enrol-be image tag (commit hash) to use
    required: false
    type: string
  epr-register-enrol-fe:
    description: epr-register-enrol-fe image tag (commit hash) to use
    required: false
    type: string
  branch:
    description: Branch in this repo to use when not a PR
    required: false
    default: main
    type: string
  journey-test-pr:
    description: Is this action being run from a PR in this repo?
    required: false
    default: 'false'
    type: string

runs:
  using: 'composite'
  steps:
    - name: Checkout repository if not called by Journey Test PR
      if: inputs.journey-test-pr == 'false'
      uses: actions/checkout@v4
      with:
        repository: DEFRA/epr-register-enrol-fe-tests
        ref: ${{ inputs.branch }}

    # ── Frontend image ─────────────────────────────────────────────────
    # Falls back to building fe main from source whenever the caller didn't
    # supply a pre-built tag (e.g. no matching branch found upstream), so we
    # never silently fall through to a possibly-stale `:latest` image.
    - name: Resolve operator-fe branch
      if: inputs.journey-test-pr != 'false' || inputs.epr-register-enrol-fe == ''
      id: operator-fe-branch
      shell: bash
      env:
        HEAD_REF: ${{ github.head_ref }}
      run: |
        BRANCH="$HEAD_REF"
        if git ls-remote --heads https://github.com/DEFRA/epr-register-enrol-frontend.git "$BRANCH" | grep -q .; then
          echo "ref=$BRANCH" >> "$GITHUB_OUTPUT"
        else
          echo "ref=main" >> "$GITHUB_OUTPUT"
        fi

    - name: Checkout operator-fe
      if: inputs.journey-test-pr != 'false' || inputs.epr-register-enrol-fe == ''
      uses: actions/checkout@v4
      with:
        repository: DEFRA/epr-register-enrol-frontend
        ref: ${{ steps.operator-fe-branch.outputs.ref }}
        path: epr-register-enrol-frontend

    - name: Build operator-fe Docker image from source
      if: inputs.journey-test-pr != 'false' || inputs.epr-register-enrol-fe == ''
      id: docker-operator-fe
      shell: bash
      run: |
        cd epr-register-enrol-frontend
        OPERATOR_FE_SHA=$(git rev-parse --short HEAD)
        docker build -t "defradigital/epr-register-enrol-frontend:${OPERATOR_FE_SHA}" .
        echo "image=$OPERATOR_FE_SHA" >> "$GITHUB_OUTPUT"

    # ── Backend image ──────────────────────────────────────────────────
    # Same fallback as above, mirrored for the backend image.
    - name: Resolve operator-be branch
      if: inputs.journey-test-pr != 'false' || inputs.epr-register-enrol-be == ''
      id: operator-be-branch
      shell: bash
      env:
        HEAD_REF: ${{ github.head_ref }}
      run: |
        BRANCH="$HEAD_REF"
        if git ls-remote --heads https://github.com/DEFRA/epr-register-enrol-backend.git "$BRANCH" | grep -q .; then
          echo "ref=$BRANCH" >> "$GITHUB_OUTPUT"
        else
          echo "ref=main" >> "$GITHUB_OUTPUT"
        fi

    - name: Checkout operator-be
      if: inputs.journey-test-pr != 'false' || inputs.epr-register-enrol-be == ''
      uses: actions/checkout@v4
      with:
        repository: DEFRA/epr-register-enrol-backend
        ref: ${{ steps.operator-be-branch.outputs.ref }}
        path: epr-register-enrol-backend

    - name: Build operator-be Docker image from source
      if: inputs.journey-test-pr != 'false' || inputs.epr-register-enrol-be == ''
      id: docker-operator-be
      shell: bash
      run: |
        cd epr-register-enrol-backend
        OPERATOR_BE_SHA=$(git rev-parse --short HEAD)
        docker build -t "defradigital/epr-register-enrol-backend:${OPERATOR_BE_SHA}" .
        echo "image=$OPERATOR_BE_SHA" >> "$GITHUB_OUTPUT"

    # ── Test runner ────────────────────────────────────────────────────
    - uses: actions/setup-node@v4
      with:
        node-version-file: .nvmrc
        cache: npm
    - run: npm install -g npm@11.12.1 --ignore-scripts
      shell: bash
    - name: Install test dependencies
      # --ignore-scripts blocks dependency lifecycle scripts (supply-chain
      # hardening), matching the `npm ci --ignore-scripts` already used by
      # check-pull-request.yml. Safe here: the install scripts in this tree
      # are the browser-driver downloaders (chromedriver/edgedriver/
      # geckodriver), unused because the suite drives the compose stack's
      # selenium-chrome container via CHROMEDRIVER_URL, and esbuild's, whose
      # binary ships in the @esbuild/<platform> package npm ci installs.
      run: npm ci --ignore-scripts
      shell: bash

    - name: Start docker compose
      shell: bash
      run: |
        EPR_REGISTER_ENROL_FRONTEND=${{ steps.docker-operator-fe.outputs.image || inputs.epr-register-enrol-fe }} \
        EPR_REGISTER_ENROL_BACKEND=${{ steps.docker-operator-be.outputs.image || inputs.epr-register-enrol-be }} \
        docker compose up --wait --wait-timeout 300 -d --quiet-pull

    - name: Run the tests
      shell: bash
      run: |
        npm run test:github
        npm run report

    - name: Collect logs
      if: always()
      shell: bash
      run: |
        docker compose logs > logs.txt
        docker ps

    - name: Upload allure report
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: allure-report
        path: ./allure-report

    - name: Upload docker compose logs
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: docker-compose-logs
        path: ./logs.txt

    - name: Check for test failures
      if: always()
      shell: bash
      run: if [ -f "FAILED" ]; then exit 1; fi
