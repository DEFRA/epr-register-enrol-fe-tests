name: Run Journey Tests on GitHub

permissions:
  checks: write
  pull-requests: write

on:
  workflow_dispatch:
  workflow_call:
jobs:
  build:
    name: Run Journey Tests
    runs-on: ubuntu-latest
    steps:
      - uses: DEFRA/cdp-node-journey-test-suite-template/run-journey-tests@747162a3f36735e1e822166cc501149a2b55617e # main
